<%= form_with(model: tweet) do |form| %>
  <div class="field">
    <%= form.label(:tweet, 'Write a tweet here: ') %>
    <%= form.text_area(:tweet) %>
  </div>

  <div class="actions">
    <%= form.submit('Post it!', class: "button is-info") %>
  </div>
<% end %>
